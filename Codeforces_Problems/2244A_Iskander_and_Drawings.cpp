#include <iostream>
#include <string>
#include <stack>
#include <algorithm>

// '*' is empty part of paper
// '#' is 1cm of drawing line
// where a continuous sequence of '#' characters forms a single line
// each second Iskander removes 1cm || one # from right
// and Yura removes 1cm from the left
// if current length of line is 1 or 2 cm means 1 or 2 '#'s next sec full remove
int main()
{
    int t;
    std::cin >> t;

    for (int i = 0; i < t; i++)
    {
        int n;
        std::cin >> n;
        std::string s[n];
    }


    return 0;
}