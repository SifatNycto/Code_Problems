#include <iostream>
#include <string>
#include <algorithm>
#include <vector>

#define all(x) (x).begin(), (x).end()

int main()
{
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    int t;
    std::cin >> t;

    int n[t];
    int answer[t];


    while (t--)
    {

    }


    return 0;
}

// 1 -> Nhoj
// 0 -> John